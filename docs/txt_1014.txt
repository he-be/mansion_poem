・レイアウトのバリエーションは後回しにする
・docs/copy.txtに実在のマンションのキャッチコピーを抽出した。今後は、マンションポエムのタイトルはこのリストから相応しいものをLLMに選ばせる必要がある。入力トークン数削減のため、ランダムに20個選択して、プロンプトに含め、この中から選ばせる必要がある。
・imgに多数の画像を追加した。リプレイ性向上のため、この画像からランダムに選び、背景画像として表示するように改修する。
・TDDで開発する。